<div class="container">
    <div class="form-group">
        <label for="menus"><h2>la Table  la Table: </h2> </label>
        <select id="menus" onchange="getSelectedValue()">
            <option value="" selected disabled>Sélectionner la table</option>
            <% Object.keys(items).forEach(function(key) { %>
                <option value="<%= key %>"><%= key %></option>
            <% }); %>
        </select>
    </div>
</div>
<script>
    
</script>